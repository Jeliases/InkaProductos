server:
  port: 8080

spring:
  application:
    name: inka-gateway
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
      routes:
        # 1. MongoDB (Catálogo rápido)
        - id: catalogo-service
          uri: http://inka-catalogo:8082
          predicates:
            - Path=/api/catalogo/**

        # 2. PostgreSQL (Soporte TI )
        - id: soporte-service
          uri: http://inka-soporte:8083
          predicates:
            - Path=/api/soporte/**

        # 3. MySQL (Monolito principal)
        - id: backend-service
          uri: http://inka-backend:8081
          predicates:
            - Path=/api/**